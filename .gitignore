# dependencies
node_modules

# yarn
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# eslint
.eslintcache

# misc
.DS_Store

# IDE
.vscode
.idea

# cli
dist

.env

# --- limit-orders ---
# Secrets: never commit. The bounty gate fails on a committed .env.
.env
.env.*
!.env.example

# Internal bounty kit — strategy, scoring, competitor notes. NOT for the public repo.
docs/kit/

# Local deployment state written by `yarn clob:bootstrap` (commit .clob.json.example instead)
.clob.json

# Recorded walkthroughs — regenerate with `yarn clob:demo`, do not commit binaries
docs/video/

# Local scratch
captures/
*.local.json
