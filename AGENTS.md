# Agent instructions

Briefing for coding agents in this app (Cursor, Claude Code, Codex). Claude Code loads it through `CLAUDE.md`.

**limit-orders** — Hardhat + Next.js template and reference client for **SaucerSwap's V3 central limit
order book** on Hedera: live market data with no wallet, on-chain onboarding (HTS association → Permit2),
EIP-712 signed orders, an HCS journal of every signed intent, and on-chain verification of every fill.

Use `yarn` unless this project was created with another package manager.

## Status

Done: the market-data terminal (`/markets`, `/market/[id]`), the typed Orderbook API client, the
onboarding checklist, the HCS journal with dry-run signing, and the `clob:*` scripts. Next: submitting
signed orders, cancelling them, and verifying fills on-chain — see `.harness/prds/`.

## Commands

```bash
yarn install
yarn next:dev                  # http://localhost:3000 — market data needs no wallet and no keys
yarn next:build
yarn next:check-types
yarn hardhat:compile
yarn hardhat:test
yarn lint
yarn format
```

## Key paths

| Path | Purpose |
| --- | --- |
| `packages/nextjs/app/` | App Router pages. `/markets` and `/market/[id]` are the market-data terminal |
| `packages/nextjs/components/scaffold-hbar/` | Wallet connect, address and network components — keep |
| `packages/nextjs/hooks/scaffold-hbar/` | `useScaffoldReadContract` / `useScaffoldWriteContract` etc. |
| `packages/nextjs/contracts/externalContracts.ts` | ABIs for contracts we do not deploy (SaucerSwap reactor, Permit2) |
| `packages/nextjs/contracts/deployedContracts.ts` | Generated by `generateTsAbis.ts` on deploy — do not edit |
| `packages/nextjs/utils/hedera/valueConversion.ts` | tinybar (8dp) vs wei (18dp) conversion — the only place that converts |
| `packages/nextjs/utils/hedera/constants.ts` | Poll intervals and gas limits for the on-chain steps |
| `packages/nextjs/lib/clob/` | Orderbook API client: config, types, http, format (money), depth |
| `packages/nextjs/lib/hedera/onboarding.ts` | The six on-chain steps, derived from chain reads |
| `packages/nextjs/lib/journal/` | HCS order-intent records; writes go through `app/api/journal` |
| `packages/nextjs/app/api/clob/` | Same-origin proxy — the Orderbook API sends no CORS headers |
| `packages/hardhat/scripts/` | Account tooling, `generateTsAbis.ts`, and `clob:bootstrap`/`status`/`fund` |
| `.harness/` | Harness v2 spec, per-increment PRDs, validators |

## Frontend contract hooks

Use hooks from `packages/nextjs/hooks/scaffold-hbar`:

- `useScaffoldReadContract` — NOT `useScaffoldContractRead`
- `useScaffoldWriteContract` — NOT `useScaffoldContractWrite`

Contract data comes from `deployedContracts.ts` (generated) and `externalContracts.ts` (manual — this is
where the SaucerSwap reactor and Permit2 go).

## Rules that are easy to get wrong

1. **Every numeric from the Orderbook API is a string.** Never `Number()` one for arithmetic. Use `bigint`
   on smallest units, or a decimal library. Money bugs are the fastest way to lose this template.
2. **Key markets on `id`, never on symbol.** Testnet has three different books all labelled "HBAR".
   Symbols are display-only, and ids differ between networks.
3. **Respect the market's own grid.** Prices must be multiples of `tickStep`, sizes multiples of
   `sizeStep` and whole `lotSize` units, and the order value must clear `minNotional` — which is in
   the quote token's smallest units, not a decimal. `validateOrder` checks all of it before the wallet
   is asked for a signature.
4. **Reconcile depth by `lastUpdateId`.** A `/ws/depth` diff may only be applied if it starts exactly
   where the last one ended; any gap means fetching a fresh snapshot. A book with a missed update looks
   fine and is wrong.
5. **Hedera values:** contract amounts are tinybars (8dp); a transaction's `value` over JSON-RPC is wei
   (18dp) — multiply tinybars by `10^10`.
6. **Never write a JWT to storage.** Memory only: no localStorage, no cookie, no logs. WebSocket URLs carry
   the token in the query string, so never log a full WS URL.
7. **Secrets stay out of the repo.** `.env`, `.clob.json` and `docs/kit/` are gitignored. The Hedera
   operator key lives only in `app/api/journal`, server-side.
8. **The chain is the record.** Onboarding state and fills are checked against the mirror node and the
   contracts, never taken from the Orderbook API's word.

## UI

- **DaisyUI** classes for layout (`btn`, `card`, …); themes are defined in `packages/nextjs/styles/globals.css`
- Imports use the `~~/` alias → `packages/nextjs/*`

## Networks

- Hardhat: `packages/hardhat/hardhat.config.ts` (`hederaTestnet`, `hederaMainnet`, local)
- Frontend: `packages/nextjs/scaffold.config.ts`

## Code style

- `UpperCamelCase` — types, components
- `lowerCamelCase` — functions, variables

## Where the API facts live

The SaucerSwap Orderbook API is only partly documented, and several documented shapes are wrong. The
verified request and response shapes, the EIP-712 order struct (read from the reactor's verified source),
the onboarding steps and every known discrepancy are recorded in the internal kit and, once written, in
`docs/integration.md` and `docs/DISCREPANCIES.md`. Do not build against a guessed shape.
